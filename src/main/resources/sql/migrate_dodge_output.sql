-- 已废弃：闪避改挂在 BuffDef（见 migrate_dodge_buff.sql）
-- 若库中仍有 SkillOutput 闪避旧列，可执行：
-- ALTER TABLE app_skill_output
--   DROP COLUMN dodge_chance,
--   DROP COLUMN skill_match_mode,
--   DROP COLUMN match_skill_school,
--   DROP COLUMN match_skill_type,
--   DROP COLUMN match_damage_element,
--   DROP COLUMN match_skill_id;
